spring:
  profiles:
    active: dev

  cloud:
    nacos:
      config:
        # Nacos服务器地址
        server-addr: 127.0.0.1:8848
        # 命名空间
        namespace: public
        # 配置文件所在的分组
        group: DEFAULT_GROUP
